<Page x:Class="sessia6.Pages.DataOutput"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:sessia6.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DataOutput">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <ListView x:Name="listProducts" Grid.Row="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="250"/>
                                <ColumnDefinition Width="250"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding naimenovanie}" FontWeight="Bold" Grid.Row="0"/>
                            <TextBlock Text="{Binding image}" Grid.Row="1"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock>
                                    <Run Text="Артикул: "/>
                                    <Run Text="{Binding articul}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Тип: "/>
                                    <Run Text="{Binding type_products.type_prod}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Количество человек на производстве: "/>
                                    <Run Text="{Binding kolvo_people_for_proizv}"/>
                                </TextBlock>
                                <TextBlock>
                                    <Run Text="Номер цеха: "/>
                                    <Run Text="{Binding number_ceh}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock>
                                    <Run Text="Минимальная стоимость для агента: "/>
                                    <Run Text="{Binding min_cost_for_agent}"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>    
        </ListView>

        <Border Padding="10" Grid.Row="1">
            <StackPanel>
                <Button Content="Удалить продукт" Height="30" Width="180" HorizontalAlignment="Right" Click="DeleteButton_Click"/>
            </StackPanel>
        </Border>
        
    </Grid>
</Page>
